"""
Application package for the SovereignRoute API.

This file makes the `app` directory a proper Python package so that
imports like `app.main` and `app.db.session` work reliably when running
under Uvicorn or Alembic.
"""

